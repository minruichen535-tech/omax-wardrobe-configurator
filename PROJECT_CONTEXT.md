# OMAX Wardrobe Configurator Project Context

本文档用于让另一台电脑上的开发者或 Codex 在没有历史聊天记录的情况下快速接手当前项目。

## 项目概述

### 项目名称

奥美斯金属衣帽间配置器 / OMAX Wardrobe Configurator

### 项目目标

本项目是一个静态前端衣帽间配置器，当前重点产品系列为“日式衣帽间”。

核心目标：

- 前端通过产品系列注册表加载不同系列的数据、图片、图标和 GLB 模型。
- 客户端访问 `/configurator/:seriesId` 进行配置。
- 管理端访问 `/admin/:seriesId` 维护同一套 Excel 数据。
- 产品数据、BOM 规则、Settings、剪尺规则尽量由 Excel 驱动。
- 日式衣帽间使用真实 GLB 模型，不使用产品组件占位几何体。

### GitHub 仓库地址

https://github.com/minruichen535-tech/omax-wardrobe-configurator.git

当前主分支：`main`

最新已知提交：

```text
af4fa21 Redirect root to Japanese closet configurator
```

### Cloudflare Pages 地址

生产地址：

https://omax-wardrobe-configurator-v2.pages.dev/

默认配置器入口：

https://omax-wardrobe-configurator-v2.pages.dev/configurator/japanese-closet

## 当前部署状态

### GitHub 是否正常

正常。项目已初始化 Git，并推送到：

```text
origin https://github.com/minruichen535-tech/omax-wardrobe-configurator.git
```

本地 Git 可能不在 PATH 中。如果 PowerShell 里直接执行 `git` 报错，可使用：

```powershell
& "C:\Program Files\Git\cmd\git.exe" status
```

### Cloudflare 是否正常

已通过 Cloudflare Pages 部署成功。

Pages 项目应使用：

- 创建方式：Pages -> Connect to Git
- GitHub 仓库：`minruichen535-tech/omax-wardrobe-configurator`
- 分支：`main`
- Framework preset：`None`
- Build command：空
- Build output directory：`/`
- Root directory：空

不要使用 Direct Upload / Upload assets 方式维护此项目。

### 默认访问入口

根路径 `/` 已通过 `_redirects` 跳转到：

```text
/configurator/japanese-closet
```

当前 `_redirects`：

```text
/ /configurator/japanese-closet 302
/configurator/* /index.html 200
/admin/* /index.html 200
/* /index.html 200
```

原因：默认 `defaultSeriesId` 仍是 `open-closet`，而 `open-closet` 当前资源不完整。若访问 `/` 不跳转，会进入 `open-closet` 并出现 Missing Model。

### 自动部署流程

1. 本地修改文件。
2. 提交到 Git。
3. 推送到 GitHub `main`。
4. Cloudflare Pages 自动拉取 GitHub 仓库并部署。

常用命令：

```powershell
& "C:\Program Files\Git\cmd\git.exe" status
& "C:\Program Files\Git\cmd\git.exe" add <file>
& "C:\Program Files\Git\cmd\git.exe" commit -m "Commit message"
& "C:\Program Files\Git\cmd\git.exe" push
```

## 技术架构

### 项目类型

当前项目是静态 React + import map 架构，不使用 Vite/TypeScript 构建链。

入口 HTML 直接加载：

```html
<script type="module" src="/src/main.js?v=entry-20260603-02"></script>
```

依赖通过 import map 从 CDN 加载：

- React
- React DOM
- Three.js
- Three.js addons
- lucide-react
- xlsx

### 主要目录结构

```text
.
├─ index.html
├─ configurator/
│  └─ index.html
├─ admin/
│  └─ index.html
├─ src/
│  ├─ main.js
│  ├─ configurator.js
│  ├─ dataSource.js
│  ├─ scene.js
│  ├─ styles.css
│  └─ config/
│     ├─ productSeries.js
│     └─ theme.js
├─ products/
│  └─ japanese-closet/
│     ├─ excel/
│     │  ├─ products.xlsx
│     │  └─ rules.xlsx
│     ├─ images/
│     │  ├─ products/
│     │  ├─ icons/
│     │  ├─ scene/
│     │  └─ detail/
│     ├─ models/
│     │  └─ glb/
│     ├─ su/
│     └─ docs/
├─ brand/
│  ├─ logo.png
│  └─ brand.json
├─ _redirects
├─ _headers
└─ vercel.json
```

### 关键文件说明

#### `src/main.js`

职责：

- React UI 主入口。
- 路由解析后加载产品系列数据。
- 渲染配置端和管理端。
- 管理房间尺寸、布局、层板深度、立柱高度、选中组件、备注输入等 UI 状态。
- 渲染左侧控制栏、组件库、属性面板、3D 场景、右侧 BOM/报价列表。

注意：

- 组件库中隐藏 `autoGenerated=true` 的组件。
- `heightLocked=true` 的 placement 不显示高度滑块。
- 不要随意修改中文 UI 文案，之前曾出现过乱码问题。

#### `src/configurator.js`

职责：

- 业务配置计算。
- 创建初始配置。
- 计算 active walls、bay、post、placement、BOM。
- 推荐跨数。
- 处理 L/U 型侧墙有效长度。
- 自动生成木顶板 placement。

关键逻辑：

- `calculateDesign(config, data)` 是核心计算入口。
- `withAutoWoodTopPlacements()` 自动为每个 active wall 的每个 bay 生成 `woodTop`。
- side wall 与 back 同时启用时：
  - `sourceLength = room.depth`
  - `startOffset = shelfDepth`
  - `length = room.depth - shelfDepth`

#### `src/scene.js`

职责：

- Three.js 3D 场景。
- 加载 GLB 模型。
- 缓存模型并 clone 使用。
- 渲染房间墙体、地面、立柱、木顶板、层板、挂衣杆、柜子、首饰盒、裤架。
- 处理模型缩放、旋转、深度锚点、微调 offset。
- 提供模型调试报告。

注意：

- 房间墙体和地面可以使用 BoxGeometry。
- 产品组件应优先使用 GLB。
- GLB 缺失时显示 Missing Model，不要用错误几何体替代。
- `sceneRuntimeVersion` 用于确认浏览器运行态是否加载了预期版本。

#### `src/dataSource.js`

职责：

- 读取 `products.xlsx`、`rules.xlsx`。
- 解析 Products、Rules、Settings。
- 管理 localStorage override。
- 导出 Products/Rules workbook。

注意：

- Settings 读取失败必须 fallback，不能导致页面空白。
- 当前 `products.xlsx` 也包含 Settings Sheet。
- 不要随意改 Products/Rules/Settings 解析，属于高风险区域。

#### `src/config/productSeries.js`

职责：

- 注册所有产品系列。
- 解析 `/configurator/:seriesId` 和 `/admin/:seriesId`。
- 根据 series 配置解析图片、GLB、Excel 路径。

当前关键配置：

```js
"japanese-closet": {
  seriesId: "japanese-closet",
  productPath: "products/japanese-closet/excel/products.xlsx",
  rulesPath: "products/japanese-closet/excel/rules.xlsx",
  assetRoot: "products/japanese-closet",
  imageRoot: "products/japanese-closet/images",
  modelRoot: "products/japanese-closet/models/glb"
}
```

当前 `defaultSeriesId = "open-closet"`，但根路径已通过 `_redirects` 跳转到日式衣帽间。

## 日式衣帽间配置器规则

### 产品系列

显示名称：日式衣帽间

seriesId：

```text
japanese-closet
```

客户端：

```text
/configurator/japanese-closet
```

管理端：

```text
/admin/japanese-closet
```

### 数据源

项目内实际读取：

```text
products/japanese-closet/excel/products.xlsx
products/japanese-closet/excel/rules.xlsx
```

外部维护源曾是：

```text
E:\网页设计资料\日式衣帽间\JAPANESS CLOSET.xlsx
```

注意：如果外部 Excel 修改后，需要明确同步到项目内 `products/japanese-closet/excel/products.xlsx` 或 `rules.xlsx`，否则线上不会变化。

### GLB 模型路径

真实 GLB 目录：

```text
products/japanese-closet/models/glb/
```

当前主要 GLB：

```text
JO-WOOD-TOP.glb
JP-CABINET.glb
JP-JEWELRY-BOX.glb
JP-POST.glb
JP-RAIL-DOUBLE.glb
JP-RAIL.glb
JP-TROUSER-RACK.glb
JP-WOOD-SHELF.glb
JP-whole.glb
```

Products Sheet 中 `modelPath` 可以只写文件名，例如：

```text
JP-POST.glb
JP-WOOD-SHELF.glb
```

`resolveSeriesAsset()` 会解析到：

```text
/products/japanese-closet/models/glb/<modelPath>
```

### 立柱规则

- 立柱使用真实 `JP-POST.glb`。
- 立柱视觉尺寸已修正为：
  - X：33mm
  - Z：100mm
  - Y：跟随 `postHeight`
- 立柱高度由 Settings 中的 `postHeightOptions` 和 `defaultPostHeight` 控制。
- 当前 Settings：
  - `roomHeightFixed = 2700`
  - `postHeightOptions = [2000, 2400]`
  - `defaultPostHeight = 2400`
  - `hideRoomHeightInput = true`

### wallOffset 规则

当前确认的产品规则：

所有墙面的立柱都应该离对应墙面固定 250mm。

Settings：

```text
fixedPostWallOffset = 250
```

3D 规则：

- back wall 沿 X 方向展开，离后墙 250mm：

```text
back group z = -roomDepth / 2 + wallOffset
```

- left wall 沿深度方向展开，离左墙 250mm：

```text
left group x = -roomWidth / 2 + wallOffset
```

- right wall 沿深度方向展开，离右墙 250mm：

```text
right group x = roomWidth / 2 - wallOffset
```

### shelfDepth 规则

旧逻辑曾把 `wallOffset` 同时作为立柱离墙距和层板深度。已拆分。

当前规则：

- `fixedPostWallOffset = 250` 控制立柱离墙距。
- `shelfDepth` 控制木顶板和木层板深度。

Settings：

```text
shelfDepthOptions = [300, 450, 500]
defaultShelfDepth = 450
```

UI 文案为：

```text
层板深度
```

只影响：

- woodTop
- woodShelf

不影响：

- post
- singleRail
- doubleRail
- cabinet
- jewelryBox
- trouserRack

### startOffset 和 side wall 规则

当存在 `back + left`、`back + right` 或 U 型时，侧墙不能从后墙平面开始。

规则：

```text
side effectiveLength = room.depth - shelfDepth
side startOffset = shelfDepth
```

数据层输出：

```text
sourceLength = room.depth
startOffset = shelfDepth
length = room.depth - shelfDepth
```

3D 视觉层：

```text
sideCenterZ = startOffset / 2
left/right group.position.z = sideCenterZ
```

这样 left/right side wall 从后墙系统占用的 shelfDepth 之后开始，避免顶板/层板在转角处完全重叠。

### 自动木顶板规则

Products Sheet 中：

```text
JP-WOOD-TOP autoGenerated = TRUE
JP-WOOD-TOP heightLocked = TRUE
```

规则：

- 每个 active wall 的每个 bay 自动生成一个 `woodTop` placement。
- ID 格式：

```text
auto:woodTop:<wallId>:<bayIndex>
```

- 自动木顶板进入 `design.placements`，因此参与模型显示和 BOM 统计。
- 自动木顶板高度跟随 `postHeight`，不是 `room.height`。
- 自动木顶板不可删除，不显示高度滑块。
- 组件库中隐藏 `autoGenerated=true` 的组件，避免用户重复拖入木顶板。

### 剪尺规则

剪尺不是直接使用 bayWidth。

逻辑：

```text
spaceWidth -> groupCount -> usableBayWidth -> cutLength
```

日式衣帽间剪尺规则：

```text
每组内宽 = (空间尺寸 / 组数) - 33mm
木顶板长度 = 每组内宽 - 5mm
木层板长度 = 每组内宽 - 5mm
挂衣杆（单）长度 = 每组内宽 - 15mm
挂衣杆（双）长度 = 每组内宽 - 15mm
柜子宽度 = 每组内宽
首饰盒：不显示剪尺
裤架：不显示剪尺
```

不要随意修改剪尺公式。

### BOM 规则

BOM/报价来自 Products + Rules。

规则：

- BOM 只显示 `sellable=true` 的销售 SKU。
- 虚拟组合件可参与规则，但本体不一定显示在报价单。
- Rules 使用：

```text
parentSku | childSku | quantity | note
```

- 所有 `childSku` 必须存在于 Products Sheet。
- BOM 按 `bomGroup` 分组。
- 分组按 `sortOrder` 升序。
- 紧固件也必须显示，即使单价为 0。
- 总价统计所有显示产品小计。

紧固件示例：

```text
JP-SCREW-4X16
JP-SCREW-4X20
JP-EXPANSION-8X65
JP-CAP
```

不要隐藏紧固件，只是默认折叠紧固件分组。

## 已修复问题

以下按大致时间顺序记录。

### 多产品系列架构

- 新增 `productSeries.js`。
- 支持 `/configurator/:seriesId` 和 `/admin/:seriesId`。
- 日式衣帽间使用 `seriesId = japanese-closet`。
- 日式系列读取：
  - `products/japanese-closet/excel/products.xlsx`
  - `products/japanese-closet/excel/rules.xlsx`

### 日式衣帽间目录结构

建立：

```text
products/japanese-closet/
  excel/
  su/
  images/
  models/glb/
  docs/
```

### 图片路径修复

- 产品图片使用相对路径。
- 禁止前端使用 `E:\...` 本地绝对路径。
- 缺图显示默认占位。

### 真实 GLB 接入

- 引入 GLTFLoader。
- 从 Products 的 `modelPath/glbAssetPath` 读取 GLB。
- 建立模型缓存，同模型多次使用时 clone。
- 产品组件优先真实 GLB。
- 缺失时显示 Missing Model。

### GLB 原点和 bbox 对齐

- 修复 GLB 原点不在中心导致组件偏移的问题。
- 增加 bbox 对齐逻辑。
- 后续发现部分模型原点是设计好的安装基准，因此增加 alignMode 诊断和小步测试，但最终 woodShelf 恢复 bboxCenter。

### componentTransformMap

为不同组件建立独立 transform 配置：

- woodTop
- woodShelf
- singleRail
- doubleRail
- cabinet
- jewelryBox
- trouserRack
- post

用于控制：

- rotation
- depthOffset
- heightOffset
- offsetX
- offsetZ
- depthAnchor

### woodShelf offset

woodShelf 经过小步测试后设置：

```text
offsetX = 0.015
depthAnchor = back
```

### cabinet / jewelryBox / trouserRack offset

经过小步校准：

```text
offsetX = 0.015
```

### rail 深度和 X 偏移

singleRail / doubleRail：

```text
depthOffset = 0
offsetX = 0
```

### woodTop 方向和高度

woodTop 当前关键配置：

```text
rotation = [0, Math.PI, 0]
offsetX = 0
depthOffset = 0
heightOffset = -0.04
depthAnchor = back
```

注意：woodTop 曾有 `depthOffset = -0.04`，后续测试改为 `0`。

### floor 插入地面

问题：

- floor 顶面在 y=20mm。
- post 底部在 y=0mm。
- 视觉上立柱插入地板 20mm。

修复：

```js
floor.position.set(0, -wallThickness / 2, 0)
```

结果：

- floor 顶面变为 y=0。
- post 底部不再插入地面。

### side wall wallOffset

曾经为了避免侧墙卡住立柱，临时将 left/right group x 改成 postDepth/2 inset。

后续确认产品规则：所有墙面的立柱离对应墙面固定 250mm。

最终恢复为：

```text
left x = -roomWidth / 2 + wallOffset
right x = roomWidth / 2 - wallOffset
```

### side wall effectiveLength

L 型 / U 型时，left/right 侧墙有效长度修复为：

```text
roomDepth - shelfDepth
```

并保留：

```text
sourceLength = roomDepth
startOffset = shelfDepth
```

### right wall post 方向

U 型 / L 型时右侧立柱视觉正反面不一致。

小步测试后确认 right wall post 额外旋转 180 度后正常。

当前逻辑：

```js
if (wall.id === "right") {
  post.rotation.y += Math.PI;
}
```

### back wall 端部立柱 visual inset

最初只在 I 型 back-only 时对后墙首尾立柱做视觉内缩。

后来发现 L/U 型时后墙最右端立柱会进入右侧墙体。

修复为：

- 只要 `wall.id === "back"`，back 首尾立柱都应用 visual inset。
- `postEndVisualInset = 25mm`
- left/right side wall 不应用该逻辑。

### woodTop edge diagnostics

新增 `woodTopDiagnostics`，用于诊断自动 woodTop 端部与墙/边界的 gap。

字段包括：

- placementId
- wallId
- bayIndex
- wallBayCount
- isFirstBay
- isLastBay
- expectedEdgeRole
- expectedBoundaryWorld
- actualEdgeWorld
- edgeGapMm
- edgeAxis
- suggestedLocalDirection

### auto woodTop edge gap 小步修复

对 `autoGenerated=true` 的 woodTop 端部 bay 做局部 X 方向视觉修正：

```text
-localX: model.position.x -= 0.019
+localX: model.position.x += 0.019
```

仅处理：

- `componentType === "woodTop"`
- `autoGenerated === true`
- `bayIndex === 0` 或 `bayIndex === wall.bayCount - 1`

不处理 middle bay，不影响 BOM/剪尺。

### Settings 扩展

Settings 当前支持：

- roomHeightFixed
- postHeightOptions
- defaultPostHeight
- hideRoomHeightInput
- wallOffsetOptions
- shelfDepthOptions
- defaultShelfDepth
- fixedPostWallOffset

Settings 读取失败时必须 fallback。

### 中文乱码修复

曾出现 `锟斤拷` 中文乱码。

定位为 `src/main.js` 中硬编码中文 UI 文案损坏。

已修复。

注意：后续编辑中文文件时确保 UTF-8。

### 品牌卡片

品牌资源：

```text
brand/logo.png
brand/brand.json
```

3D 模型区域右下角显示品牌信息卡片。

当前显示：

- logo
- brandNameCn
- brandNameEn
- seriesName
- phone

不显示：

- email
- address

### Cloudflare 部署

新增：

```text
_redirects
_headers
```

Cloudflare Pages 使用 Git 连接自动部署。

根路径 `/` 已跳转到 `/configurator/japanese-closet`，避免默认 `open-closet` 资源缺失导致 Missing Model。

## 当前已知问题

### L 型局部模型重叠

L 型 / U 型转角处仍可能存在局部视觉重叠或缝隙问题。

已经做过：

- side wall effectiveLength
- side wall startOffset
- back wall endpoint visual inset
- auto woodTop edge diagnostics
- auto woodTop 端部 gap 小步修复

但仍建议继续用诊断报告逐项校准，而不是直接大改公式。

### woodTop 端部贴墙仍需复查

auto woodTop 端部已加入 19mm 局部修正，但需要在线上部署后用：

```js
JSON.parse(document.documentElement.getAttribute("data-model-report")).woodTopDiagnostics
```

检查 edgeGapMm 是否接近 0。

### open-closet / new-system / wall-mounted 资源不完整

当前线上根路径已经跳转到日式衣帽间。

不要直接访问 `/configurator/open-closet` 并期待完整模型，因为该系列当前没有完整 GLB/Products 数据。

如果未来要启用其它系列，需要补齐：

- products.xlsx
- rules.xlsx
- images
- models/glb
- productSeries 注册信息

### 浏览器模块缓存

曾多次出现浏览器继续使用旧 `main.js` / `scene.js` 模块的情况。

当前入口 HTML 使用：

```text
/src/main.js?v=entry-20260603-02
```

如果修改 JS 后线上或本地没有变化，优先检查：

- HTML 是否带新的 query version。
- main.js 中 scene/configurator import 是否带新的 query version。
- Cloudflare 是否已完成部署。
- 浏览器是否强刷。

## 调试工具

### data-model-report

可靠读取方式：

```js
JSON.parse(document.documentElement.getAttribute("data-model-report"))
```

有时 `window.__modelLoadReport` 在某些浏览器自动化上下文里不可读，但 `data-model-report` 可靠。

### `__modelLoadReport`

如果可用，可直接读取：

```js
window.__modelLoadReport
```

包含：

- sceneRuntimeVersion
- successCount
- failedCount
- requested
- missingPlacements
- postCoordinates
- bayCoordinates
- bayPlacements
- componentDimensions
- modelBounds
- transformDiagnostics
- runtimeDebug
- woodTopDiagnostics

### woodTopDiagnostics

读取：

```js
JSON.parse(document.documentElement.getAttribute("data-model-report")).woodTopDiagnostics
```

用于判断 auto woodTop 的端部贴墙问题。

常看字段：

- placementId
- wallId
- bayIndex
- expectedEdgeRole
- edgeAxis
- expectedBoundaryWorld
- actualEdgeWorld
- edgeGapMm
- suggestedLocalDirection

### postCoordinates

读取：

```js
JSON.parse(document.documentElement.getAttribute("data-model-report")).postCoordinates
```

用于检查：

- postIndex
- localX
- visualLocalX
- worldX/worldZ
- visualWorldX/visualWorldZ

### runtimeDebug

读取：

```js
JSON.parse(document.documentElement.getAttribute("data-model-report")).runtimeDebug
```

用于确认：

- sceneRuntimeVersion
- wallOffset
- sideCenterZ
- groupPosition
- scene.js 实际运行态

### 检查 Missing Model

读取：

```js
JSON.parse(document.documentElement.getAttribute("data-model-report")).missingPlacements
```

看：

- name
- modelPath
- reason

如果 URL 返回 `text/html` 而不是 `model/gltf-binary`，通常说明路径不存在并被 Cloudflare fallback 到 `index.html`。

## 开发规范

### 不要随意修改的文件/区域

除非用户明确批准，不要随意修改：

- `src/scene.js` 模型定位逻辑
- `src/configurator.js` BOM / 剪尺 / activeWalls 计算
- `src/dataSource.js` Products / Rules / Settings 解析
- `src/main.js` 大范围 UI 文案和配置状态
- Excel 结构
- GLB 文件
- BOM 规则
- 拖拽逻辑
- 颜色逻辑
- Settings 读取逻辑

### 高风险修改前必须做什么

修改以下区域前必须先输出计划并等待确认：

- Products parsing
- Rules parsing
- Settings parsing
- main.js
- scene.js
- model loading logic
- BOM generation logic
- cut size calculation logic

计划应包含：

- 修改文件
- 风险点
- 修改范围
- 不会修改的内容
- 验证方式

### 推荐调试流程

1. 先确认当前 URL：

```js
location.href
```

2. 读取当前模型报告：

```js
const report = JSON.parse(document.documentElement.getAttribute("data-model-report"));
report.sceneRuntimeVersion
```

3. 检查 series：

- `/` 会被 Cloudflare redirect 到 `/configurator/japanese-closet`
- `/configurator/japanese-closet` 应使用 `japanese-closet`

4. 检查模型路径：

```js
report.requested
report.missingPlacements
```

5. 检查 wall/post/bay：

```js
report.runtimeDebug
report.postCoordinates
report.bayCoordinates
```

6. 检查 woodTop：

```js
report.woodTopDiagnostics
```

7. 如果代码改了但页面没变：

- 更新 import query version。
- 强刷浏览器。
- 检查 Cloudflare 最新部署是否完成。
- 检查线上返回的 JS 文件内容。

## 本地开发

项目是静态站点，可以用任意静态服务器启动。

曾使用 Python SPA fallback server。示例思路：

```powershell
cd "E:\网页设计资料\奥美斯金属衣帽间配置器-20260530-094352"
python -m http.server 4176
```

注意：普通 `http.server` 对深层路由不一定支持 SPA fallback。若访问 `/configurator/japanese-closet` 404，需要使用带 fallback 的本地服务器，或先访问 `/` 再靠 Cloudflare `_redirects` 线上验证。

本地常用地址：

```text
http://127.0.0.1:4176/configurator/japanese-closet
```

## 部署配置

### Cloudflare Pages

`_redirects`：

```text
/ /configurator/japanese-closet 302
/configurator/* /index.html 200
/admin/* /index.html 200
/* /index.html 200
```

`_headers`：

```text
/index.html
  Cache-Control: no-store

/src/*
  Cache-Control: public, max-age=0, must-revalidate

/products/*
  Cache-Control: public, max-age=0, must-revalidate

/brand/*
  Cache-Control: public, max-age=86400

/*.glb
  Cache-Control: public, max-age=86400

/*.xlsx
  Cache-Control: public, max-age=0, must-revalidate
```

### Vercel

`vercel.json`：

```json
{
  "rewrites": [
    { "source": "/configurator/:path*", "destination": "/index.html" },
    { "source": "/admin/:path*", "destination": "/index.html" },
    { "source": "/((?!.*\\.).*)", "destination": "/index.html" }
  ]
}
```

当前实际使用 Cloudflare Pages。

## 下一步建议

### 1. 验证根路径跳转

访问：

```text
https://omax-wardrobe-configurator-v2.pages.dev/
```

应跳转到：

```text
https://omax-wardrobe-configurator-v2.pages.dev/configurator/japanese-closet
```

### 2. 验证 Missing Model

打开 Console：

```js
const report = JSON.parse(document.documentElement.getAttribute("data-model-report"));
report.missingPlacements
```

日式衣帽间正常情况下不应出现核心产品组件 Missing Model。

### 3. 复查 woodTop edge gap

```js
const report = JSON.parse(document.documentElement.getAttribute("data-model-report"));
report.woodTopDiagnostics
  .filter(x => x.autoGenerated && x.edgeGapMm !== null && x.expectedEdgeRole !== "middle")
  .map(x => ({
    placementId: x.placementId,
    wallId: x.wallId,
    bayIndex: x.bayIndex,
    expectedEdgeRole: x.expectedEdgeRole,
    edgeGapMm: x.edgeGapMm
  }));
```

目标：端部 gap 接近 0mm。

### 4. 不要急着修 L/U 型转角

L/U 型转角问题已经涉及：

- wallOffset
- startOffset
- side wall effectiveLength
- post direction
- back endpoint visual inset
- auto woodTop edge offset

建议以后继续小步诊断，不要一次性重写 scene positioning。

### 5. 如果要启用其它系列

必须补齐对应目录：

```text
products/<seriesId>/excel/products.xlsx
products/<seriesId>/excel/rules.xlsx
products/<seriesId>/images/
products/<seriesId>/models/glb/
```

并在：

```text
src/config/productSeries.js
```

登记该系列。

### 6. 换电脑接手步骤

1. Clone 仓库：

```bash
git clone https://github.com/minruichen535-tech/omax-wardrobe-configurator.git
```

2. 进入项目：

```bash
cd omax-wardrobe-configurator
```

3. 用静态服务器打开。

4. 访问：

```text
/configurator/japanese-closet
```

5. 优先阅读：

```text
PROJECT_CONTEXT.md
src/config/productSeries.js
src/configurator.js
src/scene.js
src/dataSource.js
```

6. 修改前先确认是否属于高风险区域。

