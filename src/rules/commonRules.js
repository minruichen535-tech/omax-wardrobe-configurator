export const PLAN_LEVELS = Object.freeze({
  basic: 0.70,
  value: 0.85,
  premium: 0.95
});

export const DEFAULT_BAY_WIDTH = 900;
export const MIN_BAY_WIDTH = 400;
export const MAX_BAY_WIDTH = 1200;

export const DEFAULT_ZONE_PRIORITY = Object.freeze([
  "longHangZone",
  "shortHangZone",
  "shoeZone",
  "bagZone",
  "beddingZone",
  "luggageZone",
  "trouserZone",
  "jewelryZone",
  "displayZone"
]);
